import React, { useState } from 'react';
import styled from 'styled-components';
import AdminUI from './Ui/AdminUI';

const Admin = () => {
    return (
        <AdminUI>
            <h1>Dashboard</h1>
            <p>Здесь пока ничего нет, но сущность отвечающая за разного рода уведомления в бэке существует. Нужно дописать API для работы с ним как на бэке так и на фронте.</p>
        </AdminUI>
    )
}
export default Admin